#include "main.h"

/**
 * mul - the function to call for multiplication
 * @a: first integer
 * @b: second integer
 *
 * Return: this will return the result of the multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
