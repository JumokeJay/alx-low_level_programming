#include "main.h"
#include <stdio.h>
/**
 * main - the function og the program
 * @argc: the count of the argument
 * @argv: the array of the arguments
 * Return: the result of the code
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
