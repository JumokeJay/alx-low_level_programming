#include "main.h"
#include <stdio.h>
/**
 * add - sums up the two integers
 * @i: the first int
 * @j: the second int
 * Return: to produce the sum
 */
int add(int i, int j)
{
int result;
result = i + j;

return (result);
}
